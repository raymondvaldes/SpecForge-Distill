---
phase: 06-cross-platform-runtime-ergonomics
plan: 02
type: execute
wave: 2
depends_on: ["06-01"]
files_modified:
  - src/specforge_distill/cli.py
  - src/specforge_distill/automation.py
  - src/specforge_distill/ingest/pdf_loader.py
  - README.md
  - docs/TROUBLESHOOTING.md
  - tests/phase1/test_ingest_and_quality.py
  - tests/reliability/test_stress_and_robustness.py
autonomous: true
requirements: [PLAT-03, CLI-04]
user_setup: []
must_haves:
  truths:
    - "Runtime failures distinguish bad invocation/path problems, malformed PDF problems, likely low-text or scanned-input problems, and successful runs that simply extracted little or no useful content."
    - "Error handling remains deterministic and compatible with the documented CLI/output contract."
    - "Platform-specific path and permissions guidance is explicit enough for WSL, Ubuntu, macOS, and PowerShell 7 users to recover without guessing."
    - "Runtime classification and recovery logic is refactored into maintainable decision points instead of being spread across ad hoc conditionals."
  artifacts:
    - path: "src/specforge_distill/cli.py"
      provides: "User-facing runtime error and warning behavior"
    - path: "src/specforge_distill/automation.py"
      provides: "Stable machine-readable failure-class contract"
    - path: "docs/TROUBLESHOOTING.md"
      provides: "Failure-class-first runtime recovery guidance"
    - path: "tests/phase1/test_ingest_and_quality.py"
      provides: "CLI contract coverage for runtime and self-test outputs"
    - path: "tests/reliability/test_stress_and_robustness.py"
      provides: "Runtime robustness coverage for malformed inputs and edge conditions"
  key_links:
    - from: "src/specforge_distill/cli.py"
      to: "src/specforge_distill/automation.py"
      via: "CLI runtime failures stay aligned with the documented failure-class vocabulary"
      pattern: "failure_class"
    - from: "docs/TROUBLESHOOTING.md"
      to: "src/specforge_distill/cli.py"
      via: "Troubleshooting routes users based on the exact runtime outcomes the CLI surfaces"
      pattern: "Failure Class"
    - from: "tests/phase1/test_ingest_and_quality.py"
      to: "src/specforge_distill/automation.py"
      via: "Tests enforce the stable output and failure contract"
      pattern: "describe_output"
---

<objective>
Harden runtime error handling so users can tell the difference between platform/path mistakes, malformed PDFs, likely image-only inputs, and valid runs with sparse extraction, while refactoring the logic for maintainability.

Purpose: Make the CLI behavior more recoverable and less ambiguous before expanding into broader ingestion work.
Output: Clearer runtime failure classes, refactored runtime decision logic, improved path/permission guidance, and deterministic tests for malformed-input and empty-output boundaries.
</objective>

<execution_context>
@../../.codex/get-shit-done/workflows/execute-plan.md
@../../.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/06-cross-platform-runtime-ergonomics/06-RESEARCH.md
@src/specforge_distill/cli.py
@src/specforge_distill/automation.py
@src/specforge_distill/ingest/pdf_loader.py
@README.md
@docs/TROUBLESHOOTING.md
@tests/phase1/test_ingest_and_quality.py
@tests/reliability/test_stress_and_robustness.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: Refine runtime failure taxonomy without breaking determinism</name>
  <files>src/specforge_distill/cli.py, src/specforge_distill/automation.py, src/specforge_distill/ingest/pdf_loader.py</files>
  <action>
    - Separate malformed/corrupt/encrypted PDF failures from low-text or likely image-only inputs as far as current extractor signals allow, while preserving the deterministic CLI contract.
    - Make successful but empty or near-empty extraction results explicit so they do not look indistinguishable from processing failure.
    - Refactor the runtime classification logic into clearer helper boundaries so failure mapping and recovery guidance remain maintainable as later ingestion work expands.
    - Keep richer OCR heuristics out of scope for this phase; Phase 6 should establish the runtime boundary and messaging, not a full OCR subsystem.
  </action>
  <verify>
    <automated>PYTHONPATH=src .venv/bin/python3 -m pytest tests/phase1/test_ingest_and_quality.py -k "describe_output or self_test or dry_run"</automated>
    <manual>Exercise one malformed-PDF case and one low-text fixture case, then confirm the stderr/stdout behavior leads the user to two different recovery paths.</manual>
    <sampling_rate>After implementation</sampling_rate>
  </verify>
  <done>Runtime output makes it clear whether the problem is the file, the text layer, or simply sparse extraction from an otherwise valid run.</done>
</task>

<task type="auto">
  <name>Task 2: Improve path and permission recovery guidance for cross-platform users</name>
  <files>src/specforge_distill/cli.py, docs/TROUBLESHOOTING.md, README.md</files>
  <action>
    - Catch and message common runtime failures more explicitly: missing input, unwritable output directory, permission errors, and shell/path mismatches that commonly affect WSL and PowerShell 7 users.
    - Keep guidance short in the CLI, but route detailed recovery into troubleshooting so the user can move from symptom to fix quickly.
    - Consolidate duplicated path and permission messaging where it improves maintainability, so future platforms or failure classes do not require editing scattered branches.
    - Ensure wording does not assume Unix-only behavior when the same issue can present differently on Windows or WSL.
  </action>
  <verify>
    <automated>python3 verify_markdown.py README.md docs/TROUBLESHOOTING.md</automated>
    <manual>Read the troubleshooting sections for missing file, path mismatch, and permission failure; confirm each section tells WSL and PowerShell 7 users what to do next.</manual>
    <sampling_rate>After implementation</sampling_rate>
  </verify>
  <done>Platform-specific runtime recovery guidance is concrete and reachable from the CLI behavior.</done>
</task>

<task type="auto">
  <name>Task 3: Lock the runtime boundary with focused tests</name>
  <files>tests/phase1/test_ingest_and_quality.py, tests/reliability/test_stress_and_robustness.py</files>
  <action>
    - Add or revise targeted tests for malformed PDFs, low-text or likely scanned inputs, empty extraction results, and path/permission edge conditions.
    - Prefer explicit output-contract assertions over fragile string snapshots where possible.
    - Cover the refactored logic seams so maintainability improvements do not reduce confidence in the user-facing runtime behavior.
    - Keep stress and robustness coverage aligned with the user-facing runtime promises rather than synthetic edge cases that are unrelated to recovery behavior.
  </action>
  <verify>
    <automated>PYTHONPATH=src .venv/bin/python3 -m pytest tests/phase1/test_ingest_and_quality.py tests/reliability/test_stress_and_robustness.py</automated>
    <manual>None</manual>
    <sampling_rate>After implementation</sampling_rate>
  </verify>
  <done>The runtime failure and recovery boundary is encoded in automated tests and no longer depends on manual CLI inspection alone.</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] python3 verify_markdown.py README.md docs/TROUBLESHOOTING.md
- [ ] PYTHONPATH=src .venv/bin/python3 -m pytest tests/phase1/test_ingest_and_quality.py tests/reliability/test_stress_and_robustness.py
- [ ] CLI behavior distinguishes malformed input, low-text input, and empty-output success cases in a way docs can explain precisely
</verification>

<success_criteria>
- Runtime output clearly separates malformed-file failures from low-text or likely scanned-input scenarios.
- Path and permission issues route users to platform-appropriate fixes.
- Runtime classification logic is easier to extend and reason about after the refactor.
- Empty extraction results are explicit and not confused with processing failure.
- Tests protect the updated runtime boundary.
</success_criteria>

<output>
After completion, create .planning/phases/06-cross-platform-runtime-ergonomics/06-02-SUMMARY.md
</output>
