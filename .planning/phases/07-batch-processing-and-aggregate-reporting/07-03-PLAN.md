---
phase: 07-batch-processing-and-aggregate-reporting
plan: 03
type: execute
wave: 3
depends_on: ["07-01", "07-02"]
files_modified:
  - tests/phase3/test_phase3_acceptance.py
  - tests/test_v1_acceptance_final.py
  - tests/test_determinism.py
  - tests/reliability/test_stress_and_robustness.py
  - docs/TEST_SPEC.md
autonomous: true
requirements: [CLI-05, OUT-06, RUN-02]
user_setup: []
must_haves:
  truths:
    - "Mixed-success batch runs keep successful outputs, record failures explicitly, and still exit non-zero."
    - "Batch ordering, output naming, and aggregate summaries remain deterministic across repeated runs and naming-collision cases."
    - "Robustness and performance checks stay scaling-oriented and fast rather than relying on machine-sensitive timing thresholds."
    - "The IV&V mapping explicitly shows which tests prove the Phase 7 batch contract."
  artifacts:
    - path: "tests/phase3/test_phase3_acceptance.py"
      provides: "Package-level acceptance coverage for batch outputs and partial-failure behavior"
    - path: "tests/test_v1_acceptance_final.py"
      provides: "Milestone-level validation that the supported batch workflow behaves as a user would expect"
    - path: "tests/test_determinism.py"
      provides: "Deterministic ordering, naming, and summary stability coverage for batch execution"
    - path: "tests/reliability/test_stress_and_robustness.py"
      provides: "Failure-handling, path-edge, and scaling-oriented robustness coverage for batch mode"
    - path: "docs/TEST_SPEC.md"
      provides: "Traceability from Phase 7 requirements to the test suite responsible for executing IV&V"
  key_links:
    - from: "tests/test_determinism.py"
      to: "src/specforge_distill/batch.py"
      via: "Determinism tests enforce stable input ordering and collision-safe naming decisions"
      pattern: "batch"
    - from: "tests/reliability/test_stress_and_robustness.py"
      to: "src/specforge_distill/cli.py"
      via: "Reliability tests assert batch partial-failure behavior without relying on manual console inspection"
      pattern: "exit_code"
    - from: "docs/TEST_SPEC.md"
      to: "tests/test_v1_acceptance_final.py"
      via: "The IV&V mapping names the concrete tests that validate the batch user workflow"
      pattern: "batch"
---

<objective>
Harden the new batch workflow with explicit acceptance, determinism, and robustness coverage so partial failures and deterministic naming remain trusted product behavior rather than incidental implementation details.

Purpose: Make the Phase 7 batch contract release-grade before the milestone moves on to broader ingestion and validation hooks.
Output: Coverage for mixed-success batches, deterministic summary ordering, collision-safe output naming, and maintainable IV&V traceability for the new workflow.
</objective>

<execution_context>
@../../.codex/get-shit-done/workflows/execute-plan.md
@../../.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/07-batch-processing-and-aggregate-reporting/07-RESEARCH.md
@tests/phase3/test_phase3_acceptance.py
@tests/test_v1_acceptance_final.py
@tests/test_determinism.py
@tests/reliability/test_stress_and_robustness.py
@docs/TEST_SPEC.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add acceptance coverage for partial batch failures and preserved outputs</name>
  <files>tests/phase3/test_phase3_acceptance.py, tests/test_v1_acceptance_final.py</files>
  <action>
    - Add end-to-end acceptance coverage for a mixed batch where at least one PDF succeeds and at least one fails, then assert that successful output packages remain available and the aggregate result records the failure cleanly.
    - Cover both directory-driven and explicit multi-file invocation if that can be done without duplicating fixture setup unnecessarily.
    - Assert the supported user contract directly: output locations, summary status, per-item states, and final exit behavior.
    - Keep fixture data intentionally small so this acceptance coverage stays cheap enough to run as part of the normal full-suite loop.
  </action>
  <verify>
    <automated>PYTHONPATH=src .venv/bin/python3 -m pytest tests/phase3/test_phase3_acceptance.py tests/test_v1_acceptance_final.py</automated>
    <manual>Review one mixed-success run output tree and confirm the saved artifacts match the acceptance expectations exactly.</manual>
    <sampling_rate>After implementation</sampling_rate>
  </verify>
  <done>Partial batch failure is covered as an expected supported workflow, not just an edge case.</done>
</task>

<task type="auto">
  <name>Task 2: Add determinism and performance guardrails for batch scaling</name>
  <files>tests/test_determinism.py, tests/reliability/test_stress_and_robustness.py</files>
  <action>
    - Extend determinism coverage to batch ordering, child output-directory naming, aggregate summary ordering, and same-stem collision handling.
    - Add scaling-oriented robustness checks that verify batch helper behavior remains linear enough for small synthetic batches without depending on brittle wall-clock thresholds.
    - Prefer hermetic synthetic inputs and patched pipeline execution over heavyweight real-file loops where possible.
    - Refactor test helpers if needed so batch fixture setup remains maintainable as later phases expand ingestion and validation scope.
  </action>
  <verify>
    <automated>PYTHONPATH=src .venv/bin/python3 -m pytest tests/test_determinism.py tests/reliability/test_stress_and_robustness.py</automated>
    <manual>Inspect the slowest new batch tests and confirm they measure ordering or scaling properties rather than machine-specific raw timing.</manual>
    <sampling_rate>After implementation</sampling_rate>
  </verify>
  <done>Batch determinism and performance guardrails are explicit, fast, and maintainable.</done>
</task>

<task type="auto">
  <name>Task 3: Update IV&amp;V traceability for the Phase 7 batch workflow</name>
  <files>docs/TEST_SPEC.md</files>
  <action>
    - Extend the test mapping so `CLI-05`, `OUT-06`, and `RUN-02` are tied to the concrete batch tests added in this phase.
    - Keep the mapping specific enough that future regressions can be traced back to the exact acceptance, determinism, and robustness suites responsible for proving the batch contract.
    - Avoid inflating the IV&amp;V docs with implementation details; the purpose is requirement-to-test traceability, not a changelog.
    - If the updated mapping reveals a missing failure class or contract gap, close that gap before declaring the phase ready to ship.
  </action>
  <verify>
    <automated>python3 verify_markdown.py docs/TEST_SPEC.md</automated>
    <manual>Read the Phase 7 traceability section and confirm each requirement points to at least one acceptance-level or reliability-level test.</manual>
    <sampling_rate>After implementation</sampling_rate>
  </verify>
  <done>The Phase 7 batch workflow is explicitly represented in the repository's IV&amp;V mapping.</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] PYTHONPATH=src .venv/bin/python3 -m pytest tests/phase3/test_phase3_acceptance.py tests/test_v1_acceptance_final.py
- [ ] PYTHONPATH=src .venv/bin/python3 -m pytest tests/test_determinism.py tests/reliability/test_stress_and_robustness.py
- [ ] python3 verify_markdown.py docs/TEST_SPEC.md
- [ ] Mixed-success batch runs remain deterministic and preserve successful outputs while returning a non-zero exit code
</verification>

<success_criteria>
- Acceptance tests cover mixed-success batch execution and preserved outputs.
- Determinism tests catch output-order and naming regressions, including same-stem collisions.
- Robustness/performance checks stay fast and scaling-oriented.
- `TEST_SPEC.md` maps the Phase 7 requirements to the responsible test suites.
</success_criteria>

<output>
After completion, create .planning/phases/07-batch-processing-and-aggregate-reporting/07-03-SUMMARY.md
</output>
