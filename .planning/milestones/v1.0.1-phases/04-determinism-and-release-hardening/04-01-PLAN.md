---
phase: 04-determinism-and-release-hardening
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/specforge_distill/extract/classifier.py
  - src/specforge_distill/extract/merge.py
  - src/specforge_distill/pipeline.py
  - src/specforge_distill/render/manifest.py
  - tests/test_determinism.py
autonomous: true
requirements: [CLI-03]
must_haves:
  truths:
    - "Re-running the tool on the same input produces identical manifest.json and markdown content."
    - "Internal extraction loops use sorted iteration to ensure stable ordering."
    - "Manifest paths are relative to the output directory or execution root, not absolute."
  artifacts:
    - path: "tests/test_determinism.py"
      provides: "Automated idempotency check"
  key_links:
    - from: "src/specforge_distill/pipeline.py"
      to: "tests/test_determinism.py"
      via: "System-level test execution"
---

<objective>
Ensure that SpecForge Distill produces identical outputs for identical inputs across different runs by auditing extraction loops and implementing a determinism test harness.

Purpose: Reliability for MBSE workflows where stable IDs and ordering are critical for version control.
Output: Hardened extraction code and an automated idempotency test.
</objective>

<execution_context>
@/Users/raymondvaldes/.gemini/get-shit-done/workflows/execute-plan.md
@/Users/raymondvaldes/.gemini/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-determinism-and-release-hardening/04-RESEARCH.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Audit extraction loops and path handling</name>
  <files>
    - src/specforge_distill/extract/classifier.py
    - src/specforge_distill/extract/merge.py
    - src/specforge_distill/pipeline.py
    - src/specforge_distill/render/manifest.py
  </files>
  <action>
    - Scan for iterations over `items()`, `keys()`, and `set()` that contribute to the final list of candidates or artifacts.
    - Wrap these iterations in `sorted()` to ensure stable ordering.
    - Specifically check `classifier.py` for set iteration and `merge.py` for linking order.
    - Update `ManifestWriter` in `render/manifest.py` to ensure `source_pdf` and other paths are relative to the output directory or a stable root (e.g. using `os.path.relpath`).
  </action>
  <verify>
    <automated>grep -r "sorted(" src/specforge_distill/extract/ | wc -l</automated>
    <manual>Check manifest.json for relative paths.</manual>
    <sampling_rate>After implementation</sampling_rate>
  </verify>
  <done>Extraction loops are sorted and manifest paths are relative.</done>
</task>

<task type="auto">
  <name>Task 2: Implement determinism test harness</name>
  <files>tests/test_determinism.py</files>
  <action>
    Create a test that:
    1. Runs the pipeline twice on `fixtures/specs/sample-digital.pdf`.
    2. Writes both outputs to temporary directories.
    3. Compares the `manifest.json` files for exact equality (ignoring timestamp if present).
    4. Compares the generated `.md` files for exact byte equality.
    5. Asserts that all Requirement and Artifact IDs are identical across runs.
  </action>
  <verify>
    <automated>pytest tests/test_determinism.py</automated>
    <manual>None</manual>
    <sampling_rate>After implementation</sampling_rate>
  </verify>
  <done>Idempotency is verified through automated tests.</done>
</task>

</tasks>

<verification>
Run `pytest tests/test_determinism.py` to confirm stable behavior.
Check `manifest.json` for relative paths manually or via a script.
</verification>

<success_criteria>
- Two consecutive runs on the same file produce identical Markdown and JSON outputs.
- No absolute paths remain in the manifest.
</success_criteria>

<output>
After completion, create `.planning/phases/04-determinism-and-release-hardening/04-01-SUMMARY.md`
</output>
