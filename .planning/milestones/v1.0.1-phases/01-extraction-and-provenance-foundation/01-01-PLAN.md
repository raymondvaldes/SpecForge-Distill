---
phase: 01-extraction-and-provenance-foundation
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - pyproject.toml
  - src/specforge_distill/__init__.py
  - src/specforge_distill/cli.py
  - src/specforge_distill/pipeline.py
  - src/specforge_distill/ingest/pdf_loader.py
  - src/specforge_distill/ingest/text_quality.py
  - src/specforge_distill/config/obligation_verbs.yml
  - tests/phase1/test_ingest_and_quality.py
autonomous: true
requirements: ["ING-01", "ING-02"]
user_setup: []
must_haves:
  truths:
    - "User can run distill on a single digital-text PDF and trigger extraction pipeline startup."
    - "Tool warns when low text-layer quality is detected on specific pages."
  artifacts:
    - "src/specforge_distill/cli.py"
    - "src/specforge_distill/ingest/pdf_loader.py"
    - "src/specforge_distill/ingest/text_quality.py"
    - "src/specforge_distill/config/obligation_verbs.yml"
  key_links:
    - "CLI command invokes pipeline ingest entrypoint."
    - "Ingest flow returns page text metrics consumed by quality warning logic."
    - "External obligation taxonomy file is loaded by pipeline config at runtime."
---

<objective>
Create the Phase 1 foundation for running `distill <file.pdf>` with deterministic PDF ingest and text-layer quality diagnostics.

Purpose: Deliver reliable entrypoint behavior and quality warnings before extraction channels are layered on.
Output: CLI command, ingest modules, external verb taxonomy config, and baseline tests.
</objective>

<execution_context>
@/Users/raymondvaldes/.codex/get-shit-done/workflows/execute-plan.md
@/Users/raymondvaldes/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-extraction-and-provenance-foundation/01-CONTEXT.md
@.planning/phases/01-extraction-and-provenance-foundation/01-RESEARCH.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Scaffold package, CLI command, and external taxonomy config</name>
  <files>pyproject.toml, src/specforge_distill/__init__.py, src/specforge_distill/cli.py, src/specforge_distill/config/obligation_verbs.yml</files>
  <action>Create Python package scaffolding and implement `distill` command signature with required PDF path argument. Add external taxonomy config file with initial modal baseline and explicit version metadata. Wire config loading through a typed helper (no hardcoded verb list in code constants) to honor locked user decisions.</action>
  <verify><automated>python -m pytest tests/phase1/test_ingest_and_quality.py::test_loads_external_taxonomy_version -q</automated></verify>
  <done>CLI parses input path successfully, taxonomy config loads from file, and taxonomy version is available to pipeline runtime metadata.</done>
</task>

<task type="auto">
  <name>Task 2: Implement PDF loader and low-text quality diagnostics</name>
  <files>src/specforge_distill/pipeline.py, src/specforge_distill/ingest/pdf_loader.py, src/specforge_distill/ingest/text_quality.py</files>
  <action>Implement digital-text PDF ingest using `pypdf` and page-by-page text extraction metrics. Add deterministic quality diagnostics that flag low-character or empty-text pages and return warning payloads with page numbers. Keep behavior as warn-and-continue (not fail-fast) for Phase 1.</action>
  <verify><automated>python -m pytest tests/phase1/test_ingest_and_quality.py::test_warns_on_low_text_pages -q</automated></verify>
  <done>Running pipeline ingest returns page text records and warning objects for degraded pages while continuing execution.</done>
</task>

<task type="auto">
  <name>Task 3: Add baseline ingest and diagnostics tests</name>
  <files>tests/phase1/test_ingest_and_quality.py</files>
  <action>Create focused tests for: CLI invocation path, external taxonomy loading/version exposure, and low-text warning generation. Use deterministic fixture stubs/mocks where needed to avoid brittle PDF binary dependencies in this first plan.</action>
  <verify><automated>python -m pytest tests/phase1/test_ingest_and_quality.py -q</automated></verify>
  <done>All Phase 1 ingest diagnostics tests pass and fail clearly when quality warning behavior regresses.</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] `python -m pytest tests/phase1/test_ingest_and_quality.py -q`
- [ ] `python -m specforge_distill.cli distill fixtures/specs/sample-digital.pdf --dry-run` exits successfully (or equivalent module invocation)
- [ ] Warning payload includes page numbers for low-text pages
</verification>

<success_criteria>

- Distill CLI foundation exists and is callable with a PDF input path
- Low-text-quality warning mechanism exists and is test-covered
- External obligation taxonomy file and version are loaded from config
- No hardcoded obligation list in core ingest modules

</success_criteria>

<output>
After completion, create `.planning/phases/01-extraction-and-provenance-foundation/01-01-SUMMARY.md`
</output>
