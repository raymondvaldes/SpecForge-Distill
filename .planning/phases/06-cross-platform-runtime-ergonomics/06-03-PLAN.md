---
phase: 06-cross-platform-runtime-ergonomics
plan: 03
type: execute
wave: 3
depends_on: ["06-01", "06-02"]
files_modified:
  - tests/reliability/test_stress_and_robustness.py
  - tests/test_determinism.py
  - tests/test_v1_acceptance_final.py
  - tests/reliability/test_release_and_wrapper_contracts.py
  - docs/TEST_SPEC.md
  - docs/TEST_IVV_VISION.md
autonomous: true
requirements: [PLAT-02, PLAT-03, CLI-04]
user_setup: []
must_haves:
  truths:
    - "Performance and robustness tests protect scaling behavior and runtime guarantees without depending on brittle machine-specific timing ceilings."
    - "Determinism and acceptance tests are hermetic, do not mutate repository fixtures, and stay quiet unless they fail."
    - "The written IV&V/test-spec documents reflect the runtime and reliability boundaries introduced in Phase 6."
    - "Test support code and repeated contract setup are refactored where needed so the suite stays maintainable as runtime logic grows."
  artifacts:
    - path: "tests/reliability/test_stress_and_robustness.py"
      provides: "Robustness and scaling coverage for runtime-adjacent behaviors"
    - path: "tests/test_determinism.py"
      provides: "Hermetic determinism coverage for output-package stability"
    - path: "tests/reliability/test_release_and_wrapper_contracts.py"
      provides: "Cheaper contract validation for repeated release and wrapper checks"
    - path: "docs/TEST_SPEC.md"
      provides: "Traceability map from tests to system responsibilities"
    - path: "docs/TEST_IVV_VISION.md"
      provides: "High-level definition of what the IV&V process is meant to prove"
  key_links:
    - from: "docs/TEST_SPEC.md"
      to: "docs/TEST_IVV_VISION.md"
      via: "The detailed test map stays aligned with the higher-level IV&V purpose"
      pattern: "TEST_IVV_VISION"
    - from: "tests/test_determinism.py"
      to: "tests/reliability/test_stress_and_robustness.py"
      via: "Determinism and robustness tests together define the runtime quality boundary"
      pattern: "determin"
    - from: "tests/reliability/test_release_and_wrapper_contracts.py"
      to: "tests/reliability/test_wrapper_and_fixture_contracts.py"
      via: "Release, wrapper, and runtime contracts remain coordinated rather than drifting into separate test dialects"
      pattern: "wrapper"
---

<objective>
Finish the IV&V hardening pass so the runtime ergonomics work ships with stronger, cheaper, and more defensible tests, including refactors that improve maintainability of the verification logic.

Purpose: Convert the open test follow-up into executable coverage that matches the repository's new IV&V vision before Phase 6 is declared done.
Output: Hermetic determinism tests, performance-aware robustness tests, reduced test noise, targeted test-side refactors for maintainability, and updated IV&V traceability docs.
</objective>

<execution_context>
@../../.codex/get-shit-done/workflows/execute-plan.md
@../../.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/06-cross-platform-runtime-ergonomics/06-RESEARCH.md
@docs/TEST_IVV_VISION.md
@docs/TEST_SPEC.md
@tests/reliability/test_stress_and_robustness.py
@tests/test_determinism.py
@tests/test_v1_acceptance_final.py
@tests/reliability/test_release_and_wrapper_contracts.py
@tests/reliability/test_wrapper_and_fixture_contracts.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: Rework brittle robustness and performance tests into contract-driven checks</name>
  <files>tests/reliability/test_stress_and_robustness.py</files>
  <action>
    - Replace oversized synthetic inputs and absolute wall-clock expectations with scaling-oriented assertions that still catch quadratic or catastrophic regressions.
    - Keep malformed-input and concurrency coverage focused on output integrity and runtime behavior rather than incidental timing luck.
    - Refactor shared synthetic-input and timing helpers where that improves readability and keeps the test logic easy to evolve.
    - Preserve the intent of the existing tests while making them cheaper and less machine-sensitive.
  </action>
  <verify>
    <automated>PYTHONPATH=src .venv/bin/python3 -m pytest tests/reliability/test_stress_and_robustness.py</automated>
    <manual>Review the test assertions and confirm they compare behavior and scaling characteristics, not arbitrary hardware-specific timing budgets.</manual>
    <sampling_rate>After implementation</sampling_rate>
  </verify>
  <done>Robustness and performance coverage still catches serious regressions without producing flaky machine-dependent failures.</done>
</task>

<task type="auto">
  <name>Task 2: Make determinism and acceptance coverage hermetic and quiet</name>
  <files>tests/test_determinism.py, tests/test_v1_acceptance_final.py, tests/reliability/test_release_and_wrapper_contracts.py</files>
  <action>
    - Remove any test behavior that mutates repository fixtures or depends on shared state between invocations.
    - Replace noisy diagnostic prints with assertions, and cache repeated test-side imports or parsed workflow payloads when that improves test cost without hiding behavior.
    - Refactor test setup code where needed so determinism, acceptance, and contract tests express intent clearly instead of repeating incidental scaffolding.
    - Keep the release and wrapper contract tests focused on invariants so repeated file parsing and dynamic imports do not become unnecessary suite overhead.
  </action>
  <verify>
    <automated>PYTHONPATH=src .venv/bin/python3 -m pytest tests/test_determinism.py tests/test_v1_acceptance_final.py tests/reliability/test_release_and_wrapper_contracts.py</automated>
    <manual>None</manual>
    <sampling_rate>After implementation</sampling_rate>
  </verify>
  <done>Determinism, acceptance, and contract tests are hermetic, quieter, and cheaper to run repeatedly.</done>
</task>

<task type="auto">
  <name>Task 3: Update IV&V traceability docs to match the hardened suite</name>
  <files>docs/TEST_SPEC.md, docs/TEST_IVV_VISION.md</files>
  <action>
    - Expand the test specification so the reliability, wrapper, release-contract, determinism, and runtime-boundary tests are explicitly mapped into the Phase 6 verification story.
    - Refine the IV&V vision document only where needed so it accurately describes the evidence the suite now produces after the runtime ergonomics work lands.
    - Keep the docs descriptive of the executable test process; do not let them become aspirational prose that diverges from the suite.
  </action>
  <verify>
    <automated>python3 verify_markdown.py docs/TEST_SPEC.md docs/TEST_IVV_VISION.md</automated>
    <manual>Read both docs together and confirm the test map is a concrete instantiation of the IV&V vision rather than a separate taxonomy.</manual>
    <sampling_rate>After implementation</sampling_rate>
  </verify>
  <done>The written IV&V/test-spec narrative matches the actual runtime and reliability coverage in the suite.</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] PYTHONPATH=src .venv/bin/python3 -m pytest tests/reliability/test_stress_and_robustness.py
- [ ] PYTHONPATH=src .venv/bin/python3 -m pytest tests/test_determinism.py tests/test_v1_acceptance_final.py tests/reliability/test_release_and_wrapper_contracts.py
- [ ] python3 verify_markdown.py docs/TEST_SPEC.md docs/TEST_IVV_VISION.md
- [ ] Test expectations now encode scaling, determinism, and runtime invariants without repo-mutation side effects
</verification>

<success_criteria>
- Performance and robustness tests are resilient to machine variance while still catching real regressions.
- Determinism and acceptance tests are hermetic and noise-free.
- Release, wrapper, and runtime contract tests stay cheap enough to run often.
- The supporting test code is easier to maintain after targeted refactors.
- IV&V documentation reflects the actual Phase 6 verification boundary.
</success_criteria>

<output>
After completion, create .planning/phases/06-cross-platform-runtime-ergonomics/06-03-SUMMARY.md
</output>
