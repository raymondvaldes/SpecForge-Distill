---
phase: 05-trusted-distribution-and-install-verification
plan: 03
type: execute
wave: 3
depends_on: ["05-01", "05-02"]
files_modified:
  - .github/workflows/release.yml
  - scripts/render_release_notes.py
  - docs/RELEASE_NOTES_v1.1.0.md
  - tests/reliability/test_release_and_wrapper_contracts.py
autonomous: true
requirements: [REL-01, REL-03]
user_setup: []
must_haves:
  truths:
    - "Release publication includes a platform and CPU selection matrix that points users to the correct official asset."
    - "Release notes tell users to verify checksum, run `--version`, and run `--self-test` before first real use."
    - "Artifact publishing and release-note trust guidance stay synchronized through automation instead of manual copy-paste."
  artifacts:
    - path: ".github/workflows/release.yml"
      provides: "Automated release publication using generated notes and checksum assets"
    - path: "scripts/render_release_notes.py"
      provides: "Deterministic release-note body generation from checked-in inputs"
    - path: "docs/RELEASE_NOTES_v1.1.0.md"
      provides: "Curated v1.1.0 release content feeding the GitHub release body"
    - path: "tests/reliability/test_release_and_wrapper_contracts.py"
      provides: "Contract coverage for release-note publication inputs"
  key_links:
    - from: ".github/workflows/release.yml"
      to: "scripts/render_release_notes.py"
      via: "Workflow renders a trust-first release body from checked-in inputs"
      pattern: "render_release_notes.py"
    - from: "scripts/render_release_notes.py"
      to: "docs/RELEASE_NOTES_v1.1.0.md"
      via: "Generated release body incorporates the maintained v1.1.0 release draft"
      pattern: "RELEASE_NOTES_v1.1.0.md"
    - from: "tests/reliability/test_release_and_wrapper_contracts.py"
      to: ".github/workflows/release.yml"
      via: "Reliability tests enforce release-note automation inputs"
      pattern: "release.yml"
---

<objective>
Automate trust-first release publication so release notes and published artifacts stay aligned.

Purpose: Remove manual drift from the last mile of release publication and make the GitHub release page itself part of the verified install path.
Output: A generated release body with a platform matrix, verification steps, and synchronized artifact publication checks.
</objective>

<execution_context>
@../../.codex/get-shit-done/workflows/execute-plan.md
@../../.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05-trusted-distribution-and-install-verification/05-CONTEXT.md
@.planning/phases/05-trusted-distribution-and-install-verification/05-RESEARCH.md
@.github/workflows/release.yml
@docs/RELEASE_NOTES_v1.1.0.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create a generated trust-first release body</name>
  <files>scripts/render_release_notes.py, docs/RELEASE_NOTES_v1.1.0.md</files>
  <action>
    - Add a checked-in renderer or templating script that combines the maintained `docs/RELEASE_NOTES_v1.1.0.md` content with deterministic release metadata from 05-01.
    - Ensure the generated body includes: a platform/CPU matrix, the official GitHub Releases-only note, checksum verification, `--version`, `--self-test`, and any held-back platform disclosure if present.
    - Keep human-authored change notes curated in the draft file; only automate the trust/install matrix and deterministic release metadata sections.
  </action>
  <verify>
    <automated>python3 scripts/render_release_notes.py --version v1.1.0 --check</automated>
    <manual>Inspect the rendered output and confirm the trust/install section is at the top and matches the current asset contract.</manual>
    <sampling_rate>After implementation</sampling_rate>
  </verify>
  <done>A deterministic release body can be rendered from checked-in inputs without manual asset-name editing.</done>
</task>

<task type="auto">
  <name>Task 2: Wire generated notes into release publication</name>
  <files>.github/workflows/release.yml</files>
  <action>
    - Update the release workflow so the GitHub release page uses the generated release body and publishes the new aggregate `checksums.txt` asset from 05-01.
    - Make sure the published release page itself tells users how to choose the right asset and how to verify it before first use.
    - Do not replace the maintained release-notes draft with generated prose; wire in generated deterministic trust sections plus the curated change content.
  </action>
  <verify>
    <automated>python3 -c "import yaml, pathlib; yaml.safe_load(pathlib.Path('.github/workflows/release.yml').read_text())"</automated>
    <manual>Review the workflow and confirm the generated release body is used for tag-triggered releases.</manual>
    <sampling_rate>After implementation</sampling_rate>
  </verify>
  <done>The GitHub release page is generated from the same contract that defines the assets and trust steps.</done>
</task>

<task type="auto">
  <name>Task 3: Protect release-note automation with contract tests</name>
  <files>tests/reliability/test_release_and_wrapper_contracts.py</files>
  <action>
    - Add reliability tests that assert the release workflow references the release-note renderer, the current release-note draft, and the aggregate checksum asset.
    - Verify the contract markers for platform matrix content and verification guidance rather than asserting full rendered prose.
    - Keep these tests stable across content refreshes to the draft release notes.
  </action>
  <verify>
    <automated>PYTHONPATH=src .venv/bin/python3 -m pytest tests/reliability/test_release_and_wrapper_contracts.py -k "release"</automated>
    <manual>None</manual>
    <sampling_rate>After implementation</sampling_rate>
  </verify>
  <done>Release-note generation and trust publication behavior are covered by reliability tests.</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] python3 scripts/render_release_notes.py --version v1.1.0 --check
- [ ] PYTHONPATH=src .venv/bin/python3 -m pytest tests/reliability/test_release_and_wrapper_contracts.py -k "release"
- [ ] Release workflow YAML still parses cleanly
</verification>

<success_criteria>
- The GitHub release page includes a platform matrix and verification steps generated from checked-in inputs.
- Published artifacts and release-note trust guidance stay synchronized.
- Aggregate checksum publication is reflected on the release page as well as in workflow artifacts.
- Reliability tests guard the release publication contract.
</success_criteria>

<output>
After completion, create .planning/phases/05-trusted-distribution-and-install-verification/05-03-SUMMARY.md
</output>
