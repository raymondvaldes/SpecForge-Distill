---
phase: 05-trusted-distribution-and-install-verification
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - .github/workflows/release.yml
  - scripts/release_manifest.py
  - tests/reliability/test_release_and_wrapper_contracts.py
autonomous: true
requirements: [REL-01, REL-03]
user_setup:
  - Configure GitHub repository secrets for Windows code signing if live Authenticode signing is required.
  - Configure Apple Developer ID and notary secrets if live macOS signing and notarization are required.
must_haves:
  truths:
    - "Official release assets publish with explicit versioned filenames and platform-specific packaging."
    - "Each published release includes per-asset SHA256 files and one aggregate checksums manifest."
    - "If signing or notarization is enabled for a platform, failed trust validation prevents only that platform from publishing."
  artifacts:
    - path: ".github/workflows/release.yml"
      provides: "Trust-gated versioned release publication"
    - path: "scripts/release_manifest.py"
      provides: "Deterministic release naming and checksum manifest generation"
    - path: "tests/reliability/test_release_and_wrapper_contracts.py"
      provides: "Contract coverage for release asset and checksum behavior"
  key_links:
    - from: ".github/workflows/release.yml"
      to: "scripts/release_manifest.py"
      via: "Workflow uses a checked-in script to keep release naming and checksum generation deterministic"
      pattern: "release_manifest.py"
    - from: "tests/reliability/test_release_and_wrapper_contracts.py"
      to: ".github/workflows/release.yml"
      via: "Reliability tests assert the release workflow contract"
      pattern: "release.yml"
---

<objective>
Harden the release artifact contract so official binaries publish with versioned names, aggregate checksum material, and explicit per-platform trust gating.

Purpose: Establish a deterministic and auditable release foundation before changing install docs or release note automation.
Output: A release workflow and helper script that produce versioned assets, checksum manifests, and hold-back failed trust platforms without blocking unaffected ones.
</objective>

<execution_context>
@../../.codex/get-shit-done/workflows/execute-plan.md
@../../.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05-trusted-distribution-and-install-verification/05-CONTEXT.md
@.planning/phases/05-trusted-distribution-and-install-verification/05-RESEARCH.md
@.github/workflows/release.yml
@tests/reliability/test_release_and_wrapper_contracts.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: Introduce a deterministic versioned release manifest</name>
  <files>scripts/release_manifest.py, .github/workflows/release.yml</files>
  <action>
    - Create a checked-in helper script that takes the release tag/version plus the platform matrix metadata and emits deterministic release asset names, upload names, and one aggregate `checksums.txt` manifest alongside per-asset `.sha256` files.
    - Keep local build output names stable for smoke-test commands when that reduces workflow churn, but make the published asset names explicit and versioned.
    - Do not introduce "latest" alias assets, mirror URLs, or any non-GitHub release publication path.
  </action>
  <verify>
    <automated>python3 scripts/release_manifest.py --version v1.1.0 --check</automated>
    <manual>Inspect the generated names and confirm they follow the `distill-vX.Y.Z-platform-arch` pattern.</manual>
    <sampling_rate>After implementation</sampling_rate>
  </verify>
  <done>Release naming and aggregate checksum generation are centralized in a checked-in script and ready for workflow use.</done>
</task>

<task type="auto">
  <name>Task 2: Tighten trust-gated workflow publication</name>
  <files>.github/workflows/release.yml</files>
  <action>
    - Refactor the release workflow to call the manifest helper, publish versioned assets, attach `checksums.txt`, and keep per-asset `.sha256` uploads.
    - Preserve `fail-fast: false`, but ensure that if signing or notarization is enabled for a platform and its trust validation fails, that matrix job uploads nothing while unaffected platforms still publish.
    - Add a clear workflow summary for each matrix job stating whether the asset was unsigned, signed, notarized, or held back because trust validation failed.
  </action>
  <verify>
    <automated>python3 -c "import yaml, pathlib; yaml.safe_load(pathlib.Path('.github/workflows/release.yml').read_text())"</automated>
    <manual>Review the release job flow and confirm upload steps are downstream of trust validation for each platform.</manual>
    <sampling_rate>After implementation</sampling_rate>
  </verify>
  <done>The workflow publishes only trust-valid assets, adds an aggregate checksums manifest, and keeps partial platform failure behavior explicit.</done>
</task>

<task type="auto">
  <name>Task 3: Lock the release contract with reliability tests</name>
  <files>tests/reliability/test_release_and_wrapper_contracts.py</files>
  <action>
    - Extend reliability tests so they validate versioned release-name patterns, aggregate `checksums.txt` publication, and the workflow's trust-gated upload structure.
    - Keep tests focused on contract invariants, not brittle line-order assumptions in the workflow YAML.
    - Cover the "official downloads only" assumption indirectly through release workflow outputs rather than hand-maintained prose constants.
  </action>
  <verify>
    <automated>PYTHONPATH=src .venv/bin/python3 -m pytest tests/reliability/test_release_and_wrapper_contracts.py -k "release"</automated>
    <manual>None</manual>
    <sampling_rate>After implementation</sampling_rate>
  </verify>
  <done>Release trust behavior is protected by tests that fail if versioned naming, aggregate checksums, or trust gating regresses.</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] python3 scripts/release_manifest.py --version v1.1.0 --check
- [ ] PYTHONPATH=src .venv/bin/python3 -m pytest tests/reliability/test_release_and_wrapper_contracts.py -k "release"
- [ ] Release workflow YAML still parses cleanly
</verification>

<success_criteria>
- Versioned published asset names are deterministic and explicit.
- Aggregate `checksums.txt` is published in addition to per-asset hashes.
- Trust validation failures hold back only the affected platform.
- Release workflow contract tests cover the new behavior.
</success_criteria>

<output>
After completion, create .planning/phases/05-trusted-distribution-and-install-verification/05-01-SUMMARY.md
</output>
