---
phase: 02-requirement-modeling-and-obligation-detection
plan: 03
type: execute
wave: 2
depends_on: [02-01]
files_modified: [src/specforge_distill/extract/classifier.py]
autonomous: true
requirements: [REQ-03, REQ-06]

must_haves:
  truths:
    - "Obligation language (shall, must, required) is correctly classified"
    - "Ambiguous requirements are flagged as is_ambiguous=True"
    - "Reasons for ambiguity (passive voice, vague words) are recorded"
  artifacts:
    - path: "src/specforge_distill/extract/classifier.py"
      provides: "Requirement classification and ambiguity flagging logic"
  key_links:
    - from: "src/specforge_distill/extract/classifier.py"
      to: "src/specforge_distill/config/obligation_verbs.yml"
      via: "taxonomy lookups"
      pattern: "load_verbs"
---

<objective>
Implement obligation classification and ambiguity/confidence flagging for requirements.

Purpose: Distinguish between different levels of obligation (REQ-03) and flag low-confidence or ambiguous requirements for review (REQ-06).
Output: Classification logic for obligation verbs and ambiguity pattern detection.
</objective>

<execution_context>
@/Users/raymondvaldes/.gemini/get-shit-done/workflows/execute-plan.md
@/Users/raymondvaldes/.gemini/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/phases/02-requirement-modeling-and-obligation-detection/02-RESEARCH.md
@src/specforge_distill/config/obligation_verbs.yml
@src/specforge_distill/models/requirement.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: Implement obligation classification</name>
  <files>src/specforge_distill/extract/classifier.py</files>
  <action>
    Create `src/specforge_distill/extract/classifier.py`.
    Implement `classify_obligation(text: str) -> str` function.
    Load `obligation_verbs.yml` from `config/`.
    Match verbs in text (lowercased) against taxonomy (shall, must, should, required, etc.).
    Default to "neutral" if no verbs match.
  </action>
  <verify>
    <automated>python -c "from specforge_distill.extract.classifier import classify_obligation; print(classify_obligation('The system shall...'))"</automated>
    <manual>Check classification for shall, should, and neutral text</manual>
    <sampling_rate>after task</sampling_rate>
  </verify>
  <done>Obligation classification (REQ-03) uses the existing taxonomy correctly.</done>
</task>

<task type="auto">
  <name>Task 2: Implement ambiguity and low-confidence flagging</name>
  <files>src/specforge_distill/extract/classifier.py</files>
  <action>
    Implement `detect_ambiguity(text: str) -> Tuple[bool, List[str]]` function.
    Support patterns for:
    - Passive voice (e.g., "to be", "will be", "is done").
    - Vague quantifiers (e.g., "fast", "easy", "user-friendly", "efficient", "etc").
    - Low-confidence markers (e.g., "TBD", "TBR", "TBC").
    Return a tuple: `(is_ambiguous, [reasons])`.
  </action>
  <verify>
    <automated>python -c "from specforge_distill.extract.classifier import detect_ambiguity; print(detect_ambiguity('The system shall be user-friendly.'))"</automated>
    <manual>Check multiple ambiguity patterns (vague words, passive voice)</manual>
    <sampling_rate>after task</sampling_rate>
  </verify>
  <done>Ambiguity flagging (REQ-06) detects common requirement quality issues.</done>
</task>

<task type="auto">
  <name>Task 3: Implement Requirement model enrichment</name>
  <files>src/specforge_distill/extract/classifier.py</files>
  <action>
    Implement `enrich_requirement(req: Requirement) -> Requirement` function.
    Update `req.obligation` using `classify_obligation`.
    Update `req.is_ambiguous` and `req.ambiguity_reasons` using `detect_ambiguity`.
  </action>
  <verify>
    <automated>python -c "from specforge_distill.extract.classifier import enrich_requirement; from specforge_distill.models.requirement import Requirement; r = Requirement(id='1', text='It should be fast.', page=1, source_type='narrative'); enrich_requirement(r); print(r.obligation, r.is_ambiguous)"</automated>
    <manual>Verify enrichment updates model fields correctly</manual>
    <sampling_rate>after task</sampling_rate>
  </verify>
  <done>Enrichment logic correctly populates Requirement model fields from classification results.</done>
</task>

</tasks>

<success_criteria>
1. Requirements are classified as shall/must/should based on taxonomy (REQ-03).
2. Passive voice and vague words trigger ambiguity flags (REQ-06).
3. Ambiguous requirements are retained with explicit reasons rather than dropped.
</success_criteria>

<output>
After completion, create `.planning/phases/02-requirement-modeling-and-obligation-detection/02-03-SUMMARY.md`
</output>
