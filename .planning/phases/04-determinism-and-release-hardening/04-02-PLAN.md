---
phase: 04-determinism-and-release-hardening
plan: 02
type: execute
wave: 2
depends_on: ["04-01"]
files_modified:
  - tests/regression/test_regression.py
  - tests/regression/fixtures/complex_requirements.yaml
  - tests/regression/fixtures/complex_tables.yaml
autonomous: true
requirements: [CLI-03]
must_haves:
  truths:
    - "System prevents regressions in extraction quality for complex tables and obligation language."
    - "Actual pipeline output matches 'golden' reference data for key edge cases."
  artifacts:
    - path: "tests/regression/test_regression.py"
      provides: "Regression testing framework"
    - path: "tests/regression/fixtures/"
      provides: "Golden reference data"
---

<objective>
Establish a robust regression fixture suite to ensure that future changes do not degrade the quality of extraction for complex tables, captions, and obligation edge cases.

Purpose: Maintain high fidelity and trust in the extraction process over time.
Output: A regression test suite with "golden" reference files.
</objective>

<execution_context>
@/Users/raymondvaldes/.gemini/get-shit-done/workflows/execute-plan.md
@/Users/raymondvaldes/.gemini/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-determinism-and-release-hardening/04-01-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Build regression test harness</name>
  <files>tests/regression/test_regression.py</files>
  <action>
    - Create a test harness that loads "golden" results from YAML/JSON fixtures.
    - Run the pipeline on corresponding input (mocked or small PDF fragments).
    - Compare counts, IDs, and content of extracted Requirements and Artifacts against the golden references.
    - Use `pytest.mark.parametrize` to scale across multiple fixtures.
  </action>
  <verify>
    <automated>pytest tests/regression/test_regression.py</automated>
    <manual>None</manual>
    <sampling_rate>After implementation</sampling_rate>
  </verify>
  <done>Regression harness is functional and failing without fixtures.</done>
</task>

<task type="auto">
  <name>Task 2: Create complex regression fixtures</name>
  <files>
    - tests/regression/fixtures/complex_requirements.yaml
    - tests/regression/fixtures/complex_tables.yaml
  </files>
  <action>
    Define fixtures covering:
    - **Obligations**: Mixed 'shall', 'should', and 'must' in a single paragraph.
    - **Ambiguity**: Sentences with multiple passive voice markers and vague terms.
    - **Tables**: Table cells with multi-line text and specific column indices.
    - **Captions**: Requirements appearing immediately after a figure/table caption.
    - **IDs**: Requirements with and without pre-existing source IDs.
  </action>
  <verify>
    <automated>pytest tests/regression/test_regression.py</automated>
    <manual>Verify fixture content manually for completeness.</manual>
    <sampling_rate>After implementation</sampling_rate>
  </verify>
  <done>Complex edge cases are covered by regression tests.</done>
</task>

</tasks>

<verification>
Run `pytest tests/regression/test_regression.py` to confirm all fixtures pass.
</verification>

<success_criteria>
- Regression suite covers at least 5 distinct edge-case patterns.
- Tests pass against current "known good" state.
</success_criteria>

<output>
After completion, create `.planning/phases/04-determinism-and-release-hardening/04-02-SUMMARY.md`
</output>
