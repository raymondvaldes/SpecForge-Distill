---
phase: 03-output-packaging-and-interop-hooks
plan: 03-01
type: execute
wave: 1
depends_on: []
files_modified: [src/specforge_distill/render/markdown.py, tests/phase3/test_markdown_render.py]
autonomous: true
requirements: [OUT-03, OUT-04]
must_haves:
  truths:
    - "Tool can generate a consolidated markdown string containing all extracted content"
    - "Tool can generate specialized markdown strings for requirements and architecture"
    - "Markdown includes page-level citations for all artifacts"
  artifacts:
    - path: "src/specforge_distill/render/markdown.py"
      provides: "Markdown rendering logic for all output types"
    - path: "tests/phase3/test_markdown_render.py"
      provides: "Unit tests for markdown generation"
  key_links:
    - from: "src/specforge_distill/render/markdown.py"
      to: "src/specforge_distill/models/requirement.py"
      via: "import and iteration"
---

<objective>
Implement the markdown rendering engine to transform internal pipeline models into human-readable, RAG-ready documentation.

Purpose: Provide the primary user-facing deliverables for the distillation process.
Output: `MarkdownRenderer` class and associated unit tests.
</objective>

<execution_context>
@/Users/raymondvaldes/.gemini/get-shit-done/workflows/execute-plan.md
@/Users/raymondvaldes/.gemini/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/phases/03-output-packaging-and-interop-hooks/03-RESEARCH.md
@src/specforge_distill/models/requirement.py
@src/specforge_distill/models/artifacts.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: Implement MarkdownRenderer class</name>
  <files>src/specforge_distill/render/markdown.py</files>
  <action>
    Create a `MarkdownRenderer` class that accepts a `PipelineResult` (or its constituent parts).
    Implement methods for:
    - `render_full()`: Consolidated view with title, warnings, architecture, and requirements.
    - `render_requirements()`: Specialized view with only requirement blocks.
    - `render_architecture()`: Specialized view with only architecture artifacts.
    
    Ensure:
    - Semantic headers (# for titles, ## for sections).
    - Page citations formatted as `(p. N)`.
    - Requirement IDs and obligation labels are prominent.
    - Asset isolation: Each artifact block should be an atomic markdown block.
    - Handle empty states gracefully (e.g., "No requirements found").
  </action>
  <verify>
    Create a temporary test script or use a mock PipelineResult to print output and visually confirm markdown structure.
  </verify>
  <done>
    `MarkdownRenderer` can produce three distinct markdown strings from a single result set.
  </done>
</task>

<task type="auto">
  <name>Task 2: Implement markdown rendering unit tests</name>
  <files>tests/phase3/test_markdown_render.py</files>
  <action>
    Create unit tests using `pytest`.
    - Test `render_full` with a mix of requirements and artifacts.
    - Verify page citations appear in the output.
    - Verify requirement IDs appear in the output.
    - Test edge cases: No artifacts, multiple artifacts per page, large blocks.
  </action>
  <verify>
    pytest tests/phase3/test_markdown_render.py
  </verify>
  <done>
    Tests pass and cover all primary rendering modes.
  </done>
</task>

</tasks>

<verification>
Check that the generated markdown follows the "chunk-friendly" patterns described in 03-RESEARCH.md (semantic headers, isolated assets).
</verification>

<success_criteria>
1. `MarkdownRenderer` correctly transforms pipeline models into valid Markdown.
2. Consolidated and split rendering modes are supported.
3. Unit tests achieve >80% coverage for the rendering module.
</success_criteria>

<output>
After completion, create `.planning/phases/03-output-packaging-and-interop-hooks/03-01-SUMMARY.md`
</output>
