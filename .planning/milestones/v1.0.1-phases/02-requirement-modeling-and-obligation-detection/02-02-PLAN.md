---
phase: 02-requirement-modeling-and-obligation-detection
plan: 02
type: execute
wave: 2
depends_on: [02-01]
files_modified: [src/specforge_distill/extract/id_resolver.py]
autonomous: true
requirements: [REQ-04, REQ-05]

must_haves:
  truths:
    - "Source IDs like REQ-001 or [R-123] are correctly identified in text"
    - "Deterministic SHA1 hashes are generated when no source ID is found"
    - "Generated IDs are stable across multiple runs on identical input"
  artifacts:
    - path: "src/specforge_distill/extract/id_resolver.py"
      provides: "ID resolution logic (regex + deterministic hashing)"
  key_links:
    - from: "src/specforge_distill/extract/id_resolver.py"
      to: "src/specforge_distill/models/requirement.py"
      via: "ID assignment function"
      pattern: "resolve_requirement_id"
---

<objective>
Implement ID preservation (from source text) and deterministic hash-based generation for anonymous requirements.

Purpose: Ensure every requirement has a stable identity (REQ-04, REQ-05) to enable regression testing and VCRM tracking.
Output: ID resolution logic supporting regex-based source IDs and SHA1-based deterministic fallback.
</objective>

<execution_context>
@/Users/raymondvaldes/.gemini/get-shit-done/workflows/execute-plan.md
@/Users/raymondvaldes/.gemini/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/phases/02-requirement-modeling-and-obligation-detection/02-RESEARCH.md
@src/specforge_distill/models/requirement.py
@src/specforge_distill/models/candidates.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: Implement regex-based source ID detection</name>
  <files>src/specforge_distill/extract/id_resolver.py</files>
  <action>
    Create `src/specforge_distill/extract/id_resolver.py`.
    Implement `detect_source_id(text: str) -> Optional[str]` function.
    Support patterns like `REQ-001`, `[R-123]`, `3.2.1-1`.
    Return the detected ID string (stripped of brackets if applicable).
  </action>
  <verify>
    <automated>python -c "from specforge_distill.extract.id_resolver import detect_source_id; print(detect_source_id('REQ-001: The system shall...'))"</automated>
    <manual>Check multiple ID formats (bracketed, dotted, prefixed)</manual>
    <sampling_rate>after task</sampling_rate>
  </verify>
  <done>Regex-based source ID detection identifies common requirement ID patterns in text.</done>
</task>

<task type="auto">
  <name>Task 2: Implement deterministic SHA1 hashing for anonymous requirements</name>
  <files>src/specforge_distill/extract/id_resolver.py</files>
  <action>
    Implement `generate_stable_id(text: str, page: int, source_type: str) -> str` function.
    Use SHA1 from `hashlib`. Seed with `text` (normalized), `page`, and `source_type`.
    Normalize text: lower case, strip whitespace, remove special chars (leverage logic from `candidates.py` if needed).
    Format as `req-{source_type}-{page:03d}-{hash[:10]}`.
  </action>
  <verify>
    <automated>python -c "from specforge_distill.extract.id_resolver import generate_stable_id; print(generate_stable_id('Test text', 1, 'narrative'))"</automated>
    <manual>Verify same input produces same ID across multiple calls</manual>
    <sampling_rate>after task</sampling_rate>
  </verify>
  <done>Stable IDs are generated deterministically when source IDs are absent.</done>
</task>

<task type="auto">
  <name>Task 3: Implement resolve_requirement_id coordinator</name>
  <files>src/specforge_distill/extract/id_resolver.py</files>
  <action>
    Implement `resolve_requirement_id(text: str, page: int, source_type: str) -> str` coordinator.
    Priority: (1) `detect_source_id`, (2) `generate_stable_id`.
  </action>
  <verify>
    <automated>python -c "from specforge_distill.extract.id_resolver import resolve_requirement_id; print(resolve_requirement_id('REQ-123: Text', 1, 'narrative'))"</automated>
    <manual>Check both paths (found ID vs generated hash)</manual>
    <sampling_rate>after task</sampling_rate>
  </verify>
  <done>Coordinator function correctly prioritizes source IDs over generated ones.</done>
</task>

</tasks>

<success_criteria>
1. Source IDs are correctly preserved (REQ-04).
2. Anonymous requirements get stable, deterministic IDs (REQ-05).
3. Hash seed includes page and source to prevent collisions for identical text.
</success_criteria>

<output>
After completion, create `.planning/phases/02-requirement-modeling-and-obligation-detection/02-02-SUMMARY.md`
</output>
