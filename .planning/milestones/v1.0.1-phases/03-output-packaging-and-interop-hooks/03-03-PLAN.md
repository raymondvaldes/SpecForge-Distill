---
phase: 03-output-packaging-and-interop-hooks
plan: 03-03
type: execute
wave: 2
depends_on: [03-01, 03-02]
files_modified: [src/specforge_distill/cli.py, src/specforge_distill/pipeline.py, tests/phase3/test_cli_outputs.py]
autonomous: true
requirements: [CLI-01, CLI-02, RUN-01]
must_haves:
  truths:
    - "Tool creates an output directory adjacent to source PDF by default"
    - "User can specify custom output directory via -o flag"
    - "Output directory contains the full package (markdown files + manifest)"
    - "Tool defaults to local-only processing"
  artifacts:
    - path: "src/specforge_distill/cli.py"
      provides: "CLI path handling and orchestration"
    - path: "tests/phase3/test_cli_outputs.py"
      provides: "Integration tests for file system behavior"
  key_links:
    - from: "src/specforge_distill/cli.py"
      to: "src/specforge_distill/render/markdown.py"
      via: "rendering outputs"
    - from: "src/specforge_distill/cli.py"
      to: "src/specforge_distill/render/manifest.py"
      via: "writing manifest"
---

<objective>
Update the CLI and Pipeline to manage output locations and orchestrate the writing of all distilled artifacts.

Purpose: Finalize the "tool-like" behavior of SpecForge Distill by ensuring outputs are saved where the user expects.
Output: Updated CLI with path logic and output orchestration.
</objective>

<execution_context>
@/Users/raymondvaldes/.gemini/get-shit-done/workflows/execute-plan.md
@/Users/raymondvaldes/.gemini/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/phases/03-output-packaging-and-interop-hooks/03-RESEARCH.md
@src/specforge_distill/cli.py
@src/specforge_distill/pipeline.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: Implement output directory logic and orchestration</name>
  <files>src/specforge_distill/cli.py</files>
  <action>
    Update the CLI (and/or a helper in `pipeline.py`) to:
    - Calculate default output directory: `[source_name]_distilled/` adjacent to input PDF.
    - Support `-o` / `--output` override.
    - Ensure output directory exists (use `pathlib.Path.mkdir(parents=True, exist_ok=True)`).
    - Call `MarkdownRenderer` to generate the three markdown files.
    - Call `ManifestWriter` to generate `manifest.json`.
    - Print a summary of generated files to the console.
    - Add `--allow-external-ai` flag as a placeholder/no-op for RUN-01 compliance (local-only default).
  </action>
  <verify>
    Run `python -m src.specforge_distill.cli path/to/spec.pdf` and check if `spec_distilled/` is created with content.
  </verify>
  <done>
    The CLI correctly produces the full output package in the specified or default location.
  </done>
</task>

<task type="auto">
  <name>Task 2: Implement integration tests for output behavior</name>
  <files>tests/phase3/test_cli_outputs.py</files>
  <action>
    Create integration tests using `pytest` and `tmp_path`.
    - Verify default directory creation next to a mock PDF.
    - Verify `-o` override behavior.
    - Verify all 4 expected files (`_full.md`, `requirements.md`, `architecture.md`, `manifest.json`) are present and non-empty.
  </action>
  <verify>
    pytest tests/phase3/test_cli_outputs.py
  </verify>
  <done>
    File system orchestration is verified and stable.
  </done>
</task>

</tasks>

<verification>
Ensure the CLI provides clear feedback to the user upon successful completion, listing the paths to the generated artifacts.
</verification>

<success_criteria>
1. Default output path logic follows `[source]_distilled/` pattern.
2. `-o` flag overrides the default successfully.
3. All phase artifacts are generated on every successful run.
</success_criteria>

<output>
After completion, create `.planning/phases/03-output-packaging-and-interop-hooks/03-03-SUMMARY.md`
</output>
