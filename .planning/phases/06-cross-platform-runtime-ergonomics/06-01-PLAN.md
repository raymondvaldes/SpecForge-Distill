---
phase: 06-cross-platform-runtime-ergonomics
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - distill
  - distill.ps1
  - README.md
  - docs/BUILD.md
  - docs/TROUBLESHOOTING.md
  - tests/reliability/test_wrapper_and_fixture_contracts.py
autonomous: true
requirements: [PLAT-02, PLAT-03]
user_setup:
  - Have PowerShell 7 available if running the local development runner walkthrough outside automated tests.
  - Have WSL available if validating Linux-binary guidance from a Windows host.
must_haves:
  truths:
    - "Local development workflows no longer assume Bash-only entrypoints for contributors using PowerShell 7."
    - "The POSIX development runner still prefers the repository virtualenv and fails with actionable dependency guidance."
    - "Docs distinguish release-binary usage from local development-runner usage across PowerShell 7, WSL, Ubuntu, and macOS."
    - "Shared launcher logic is refactored so shell-specific entrypoints stay thin and maintainable instead of duplicating environment-selection behavior."
  artifacts:
    - path: "distill"
      provides: "POSIX development runner with deterministic environment selection"
    - path: "distill.ps1"
      provides: "PowerShell-friendly local development entrypoint"
    - path: "docs/BUILD.md"
      provides: "Cross-platform source-run guidance"
    - path: "tests/reliability/test_wrapper_and_fixture_contracts.py"
      provides: "Contract coverage for wrapper and entrypoint behavior"
  key_links:
    - from: "distill.ps1"
      to: "src/specforge_distill/cli.py"
      via: "PowerShell wrapper reaches the same CLI module path as the existing development runner"
      pattern: "specforge_distill.cli"
    - from: "tests/reliability/test_wrapper_and_fixture_contracts.py"
      to: "distill"
      via: "Reliability tests continue to validate wrapper precedence and dependency failure behavior"
      pattern: "distill"
    - from: "docs/BUILD.md"
      to: "README.md"
      via: "Contributor and user docs stay aligned on which entrypoint to use in which environment"
      pattern: "PowerShell 7"
---

<objective>
Remove Bash-only assumptions from local development usage and make the runtime entrypoints clearer across PowerShell 7, WSL, Ubuntu, and macOS, with targeted refactoring for maintainability.

Purpose: Phase 6 starts by fixing the developer and first-run ergonomics that sit in front of the CLI itself.
Output: Aligned POSIX and PowerShell development runners, refactored launcher logic that is easier to maintain, plus docs and tests that describe and verify the supported local invocation paths.
</objective>

<execution_context>
@../../.codex/get-shit-done/workflows/execute-plan.md
@../../.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/06-cross-platform-runtime-ergonomics/06-RESEARCH.md
@README.md
@docs/BUILD.md
@docs/TROUBLESHOOTING.md
@distill
@tests/reliability/test_wrapper_and_fixture_contracts.py
@pyproject.toml
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add a PowerShell-friendly local development entrypoint</name>
  <files>distill, distill.ps1</files>
  <action>
    - Keep the existing POSIX `distill` runner behavior intact where it improves portability, but remove unnecessary Bash-only assumptions from the overall contributor story by adding a PowerShell-friendly companion entrypoint.
    - Mirror the same core behavior in both runners: prefer the repository virtualenv when present, fail clearly when dependencies are missing, and invoke the CLI through the source tree without requiring a package install beyond the documented setup.
    - Refactor any shared launcher decisions so environment selection, dependency checks, and argv forwarding are expressed once per behavior instead of drifting across separate shell-specific branches.
    - Keep failure text explicit about when contributors should use the release binary versus the development runner.
  </action>
  <verify>
    <automated>PYTHONPATH=src .venv/bin/python3 -m pytest tests/reliability/test_wrapper_and_fixture_contracts.py -k "wrapper"</automated>
    <manual>On PowerShell 7, run the documented local development entrypoint from a clean checkout and confirm it resolves the repository environment instead of assuming Bash.</manual>
    <sampling_rate>After implementation</sampling_rate>
  </verify>
  <done>Repository-local development can be launched from both POSIX shells and PowerShell 7 using equivalent documented paths.</done>
</task>

<task type="auto">
  <name>Task 2: Rewrite local runtime docs around entrypoint selection</name>
  <files>README.md, docs/BUILD.md, docs/TROUBLESHOOTING.md</files>
  <action>
    - Clarify when users should use a verified release binary, when contributors should use the local development runner, and which commands apply to WSL, Ubuntu, macOS, and Windows PowerShell 7.
    - Add explicit copy-pasteable local-run examples for PowerShell 7 and WSL that do not require users to translate shell syntax mentally.
    - Document the common cross-platform pitfalls near the commands that trigger them: path separators, executable bit expectations, and shell-specific quoting.
  </action>
  <verify>
    <automated>python3 verify_markdown.py README.md docs/BUILD.md docs/TROUBLESHOOTING.md</automated>
    <manual>Read the local-run sections in order and confirm a new contributor can choose the correct entrypoint without guessing between the release binary and the repo runner.</manual>
    <sampling_rate>After implementation</sampling_rate>
  </verify>
  <done>Docs explain the runtime entrypoint story consistently across PowerShell 7 and POSIX environments.</done>
</task>

<task type="auto">
  <name>Task 3: Extend wrapper contract coverage for cross-platform entrypoints</name>
  <files>tests/reliability/test_wrapper_and_fixture_contracts.py</files>
  <action>
    - Expand wrapper contract tests to cover the PowerShell-friendly entrypoint, the repository virtualenv preference, and the dependency-missing path without relying on real platform-specific shells in CI when that would make the test brittle.
    - Keep tests focused on contract markers, argv forwarding, and error text that must remain stable for contributors.
    - Add coverage that protects the refactored launcher logic from diverging between POSIX and PowerShell entrypoints.
    - Preserve the existing sample-fixture sanity check so runtime ergonomics work stays tied to a real parseable input.
  </action>
  <verify>
    <automated>PYTHONPATH=src .venv/bin/python3 -m pytest tests/reliability/test_wrapper_and_fixture_contracts.py</automated>
    <manual>None</manual>
    <sampling_rate>After implementation</sampling_rate>
  </verify>
  <done>Wrapper and entrypoint regressions are protected by tests rather than doc review alone.</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] python3 verify_markdown.py README.md docs/BUILD.md docs/TROUBLESHOOTING.md
- [ ] PYTHONPATH=src .venv/bin/python3 -m pytest tests/reliability/test_wrapper_and_fixture_contracts.py
- [ ] Documented PowerShell 7 and WSL commands still invoke the same CLI contract as the existing development runner
</verification>

<success_criteria>
- Contributors have a PowerShell-friendly local runner path that does not assume Bash.
- Existing POSIX wrapper behavior remains deterministic and clearly documented.
- Shared launcher behavior is easier to follow and maintain after the refactor.
- Docs distinguish release-binary and local-development flows across supported platforms.
- Wrapper contract tests cover the supported entrypoint behavior.
</success_criteria>

<output>
After completion, create .planning/phases/06-cross-platform-runtime-ergonomics/06-01-SUMMARY.md
</output>
