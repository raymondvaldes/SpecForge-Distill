---
phase: 03-output-packaging-and-interop-hooks
plan: 03-04
type: execute
wave: 3
depends_on: [03-03]
files_modified: [src/specforge_distill/models/requirement.py, src/specforge_distill/models/artifacts.py, src/specforge_distill/render/manifest.py, tests/phase3/test_interop_hooks.py, tests/phase3/test_phase3_acceptance.py]
autonomous: true
requirements: [INT-01]
must_haves:
  truths:
    - "Entities include SysML v2 interop metadata hooks"
    - "The output package is complete and passes a final structural check"
  artifacts:
    - path: "src/specforge_distill/models/requirement.py"
      provides: "Updated model with interop fields"
    - path: "tests/phase3/test_phase3_acceptance.py"
      provides: "Final phase acceptance suite"
  key_links:
    - from: "src/specforge_distill/render/manifest.py"
      to: "src/specforge_distill/models/requirement.py"
      via: "rendering interop metadata"
---

<objective>
Add lightweight interop metadata hooks for SysML v2 integration and perform final phase-level verification of the output package.

Purpose: Future-proof the data model for model-based systems engineering (MBSE) toolchains.
Output: Updated models and a comprehensive phase acceptance suite.
</objective>

<execution_context>
@/Users/raymondvaldes/.gemini/get-shit-done/workflows/execute-plan.md
@/Users/raymondvaldes/.gemini/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/phases/03-output-packaging-and-interop-hooks/03-RESEARCH.md
@src/specforge_distill/models/requirement.py
@src/specforge_distill/models/artifacts.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add interop metadata to models and manifest</name>
  <files>src/specforge_distill/models/requirement.py, src/specforge_distill/models/artifacts.py, src/specforge_distill/render/manifest.py</files>
  <action>
    - Add an `interop` field (optional sub-model) to `Requirement` and `ArtifactBlock`.
    - `interop` should include: `target`, `candidate_concept`, and `mapping_status`.
    - Update `ManifestWriter` to ensure these fields are serialized into `manifest.json`.
    - Add a `model_interop_target` field to the root of the manifest (defaulting to "sysmlv2-future").
  </action>
  <verify>
    Run the tool and verify the `manifest.json` contains the new `interop` blocks for each entity.
  </verify>
  <done>
    SysML v2 interop hooks are present in the internal models and JSON output.
  </done>
</task>

<task type="auto">
  <name>Task 2: Implement Phase 3 acceptance suite</name>
  <files>tests/phase3/test_phase3_acceptance.py</files>
  <action>
    Create a comprehensive acceptance test that:
    - Runs the full pipeline on a sample PDF (or complex mock).
    - Verifies the existence of all 4 output files.
    - Verifies the manifest correctly links all requirements in the markdown files.
    - Verifies all requirement entities have the expected attributes (ID, obligation, interop).
  </action>
  <verify>
    pytest tests/phase3/test_phase3_acceptance.py
  </verify>
  <done>
    Phase 3 requirements are fully verified and integrated.
  </done>
</task>

</tasks>

<verification>
Perform a "compatibility check": ensure that every requirement in the markdown files has a corresponding entry in the `manifest.json` with matching IDs.
</verification>

<success_criteria>
1. All entities in the final output package contain SysML v2 interop metadata blocks.
2. The full output package is generated correctly for a complex input.
3. Acceptance tests pass, confirming all Phase 3 goals are met.
</success_criteria>

<output>
After completion, create `.planning/phases/03-output-packaging-and-interop-hooks/03-04-SUMMARY.md`
</output>
