---
phase: 07-batch-processing-and-aggregate-reporting
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/specforge_distill/cli.py
  - src/specforge_distill/automation.py
  - src/specforge_distill/batch.py
  - tests/phase3/test_cli_outputs.py
  - tests/reliability/test_wrapper_and_fixture_contracts.py
autonomous: true
requirements: [CLI-05, RUN-02]
user_setup: []
must_haves:
  truths:
    - "Users can enter batch mode either by providing multiple explicit PDF paths or by pointing the CLI at one directory."
    - "Resolved batch inputs are normalized and sorted deterministically before any processing starts."
    - "Single-file behavior remains stable and special modes keep their current exclusivity rules."
    - "Batch input resolution and output-directory planning are factored into shared helpers instead of being spread across ad hoc branches in cli.py."
  artifacts:
    - path: "src/specforge_distill/cli.py"
      provides: "CLI parser and dispatch contract for single-file versus batch execution"
    - path: "src/specforge_distill/batch.py"
      provides: "Deterministic input discovery, ordering, and per-input output planning"
    - path: "src/specforge_distill/automation.py"
      provides: "Updated CLI contract metadata for the new batch invocation surface"
    - path: "tests/phase3/test_cli_outputs.py"
      provides: "Parser and output-location coverage for batch invocation paths"
    - path: "tests/reliability/test_wrapper_and_fixture_contracts.py"
      provides: "Launcher and wrapper coverage for the expanded CLI contract"
  key_links:
    - from: "src/specforge_distill/cli.py"
      to: "src/specforge_distill/batch.py"
      via: "CLI dispatch hands resolved batch work to shared helpers instead of embedding path resolution logic inline"
      pattern: "batch"
    - from: "src/specforge_distill/automation.py"
      to: "src/specforge_distill/cli.py"
      via: "The machine-readable CLI contract stays aligned with the supported batch invocation forms"
      pattern: "describe_output_contract"
    - from: "tests/phase3/test_cli_outputs.py"
      to: "src/specforge_distill/cli.py"
      via: "Phase 3 tests assert that batch invocation preserves deterministic output behavior"
      pattern: "output_dir"
---

<objective>
Add a maintainable batch entry path to the CLI so users can process either a directory or an explicit list of PDFs without weakening deterministic dispatch or the current single-file behavior.

Purpose: Establish the batch-mode input and orchestration boundary before adding aggregate reporting artifacts.
Output: A refactored CLI and shared batch helper layer that resolve inputs deterministically, preserve the existing special-mode contract, and assign stable per-input output directories.
</objective>

<execution_context>
@../../.codex/get-shit-done/workflows/execute-plan.md
@../../.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/07-batch-processing-and-aggregate-reporting/07-RESEARCH.md
@src/specforge_distill/cli.py
@src/specforge_distill/automation.py
@tests/phase3/test_cli_outputs.py
@tests/reliability/test_wrapper_and_fixture_contracts.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: Refactor the CLI parser and dispatch path for batch mode</name>
  <files>src/specforge_distill/cli.py, src/specforge_distill/automation.py</files>
  <action>
    - Extend the normal distill invocation so it can accept either multiple explicit PDF paths or one `--input-dir` directory while leaving special modes (`--describe-output`, `--emit-example-output`, `--self-test`) unchanged.
    - Keep the single-file path compatible with the existing invocation contract and default output naming.
    - Update the machine-readable CLI metadata so automation clients can discover the batch invocation surface as soon as the parser changes land.
    - Refactor argument validation so normal distill-mode decisions remain readable instead of accumulating nested conditionals.
  </action>
  <verify>
    <automated>PYTHONPATH=src .venv/bin/python3 -m pytest tests/phase1/test_ingest_and_quality.py tests/phase3/test_cli_outputs.py -k "describe_output or cli"</automated>
    <manual>Run `distill` help text locally and confirm the single-file examples still read naturally alongside the new batch path.</manual>
    <sampling_rate>After implementation</sampling_rate>
  </verify>
  <done>The CLI cleanly separates single-file, batch, and existing special-mode behavior without breaking the current contract.</done>
</task>

<task type="auto">
  <name>Task 2: Introduce deterministic batch input and output planning helpers</name>
  <files>src/specforge_distill/batch.py, src/specforge_distill/cli.py</files>
  <action>
    - Create one shared helper layer responsible for resolving directory inputs, validating explicit multi-file inputs, sorting the final source list, and planning per-input output directories.
    - Keep directory scanning intentionally bounded in Phase 7: direct child PDFs only, no recursive traversal or glob semantics.
    - Implement deterministic collision handling for output directory names so same-stem inputs do not overwrite each other.
    - Keep this logic out of argparse branches so later summary and validation work can reuse it without duplicating path rules.
  </action>
  <verify>
    <automated>PYTHONPATH=src .venv/bin/python3 -m pytest tests/phase3/test_cli_outputs.py -k "batch or output_directory"</automated>
    <manual>Create a small same-stem/same-directory-name test fixture set and confirm the planned output directories are stable across repeated runs.</manual>
    <sampling_rate>After implementation</sampling_rate>
  </verify>
  <done>Batch input discovery, ordering, and output planning are deterministic and centralized in one maintainable helper layer.</done>
</task>

<task type="auto">
  <name>Task 3: Lock the new batch entry path with focused contract tests</name>
  <files>tests/phase3/test_cli_outputs.py, tests/reliability/test_wrapper_and_fixture_contracts.py</files>
  <action>
    - Add parser and dispatch tests that cover explicit multi-file input, directory mode, invalid mixed invocation, and default-vs-custom batch output roots.
    - Extend wrapper or launcher contract tests only where needed to ensure the new invocation surface stays reachable from the supported entrypoints.
    - Prefer explicit contract assertions over snapshots of full help text or brittle ordering assumptions unrelated to determinism.
    - Keep tests fast; this plan should protect interface boundaries, not introduce end-to-end batch acceptance coverage yet.
  </action>
  <verify>
    <automated>PYTHONPATH=src .venv/bin/python3 -m pytest tests/phase3/test_cli_outputs.py tests/reliability/test_wrapper_and_fixture_contracts.py</automated>
    <manual>None</manual>
    <sampling_rate>After implementation</sampling_rate>
  </verify>
  <done>The batch CLI surface is protected by targeted tests before aggregate reporting work begins.</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] PYTHONPATH=src .venv/bin/python3 -m pytest tests/phase1/test_ingest_and_quality.py tests/phase3/test_cli_outputs.py -k "describe_output or cli"
- [ ] PYTHONPATH=src .venv/bin/python3 -m pytest tests/phase3/test_cli_outputs.py tests/reliability/test_wrapper_and_fixture_contracts.py
- [ ] Batch input ordering and planned output directories are deterministic across repeated runs
</verification>

<success_criteria>
- Users can invoke batch mode through multiple PDF paths or one input directory.
- Single-file behavior and special-mode exclusivity do not regress.
- Batch input resolution and naming logic are centralized and maintainable.
- Tests fail if batch parser, directory discovery, or output planning drift.
</success_criteria>

<output>
After completion, create .planning/phases/07-batch-processing-and-aggregate-reporting/07-01-SUMMARY.md
</output>
