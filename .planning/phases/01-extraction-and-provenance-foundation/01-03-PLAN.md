---
phase: 01-extraction-and-provenance-foundation
plan: 03
type: execute
wave: 2
depends_on: ["01-01"]
files_modified:
  - src/specforge_distill/extract/tables.py
  - src/specforge_distill/extract/captions.py
  - src/specforge_distill/extract/merge.py
  - src/specforge_distill/models/candidates.py
  - src/specforge_distill/pipeline.py
  - tests/phase1/test_table_and_caption_extraction.py
autonomous: true
requirements: ["REQ-02"]
user_setup: []
must_haves:
  truths:
    - "Requirement-like statements from table cells are extracted as candidates."
    - "Requirement-like statements from caption context are extracted as candidates."
    - "Equivalent statements across source types remain linked, not prematurely deduplicated."
  artifacts:
    - "src/specforge_distill/extract/tables.py"
    - "src/specforge_distill/extract/captions.py"
    - "src/specforge_distill/extract/merge.py"
  key_links:
    - "Table and caption extractors emit source-typed candidates (`table_cell`, `caption_context`)."
    - "Merge layer links semantically equivalent candidates across narrative/table/caption channels."
    - "Pipeline aggregates all source buckets into one candidate ledger without dropping entries."
---

<objective>
Implement table and caption extraction channels plus candidate-link merge behavior.

Purpose: Meet Phase 1 requirement coverage for non-narrative requirement locations and preserve cross-source traceability.
Output: Table/caption extractors, cross-source link merge logic, and validation tests.
</objective>

<execution_context>
@/Users/raymondvaldes/.codex/get-shit-done/workflows/execute-plan.md
@/Users/raymondvaldes/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-extraction-and-provenance-foundation/01-CONTEXT.md
@.planning/phases/01-extraction-and-provenance-foundation/01-RESEARCH.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Implement table-cell candidate extraction</name>
  <files>src/specforge_distill/extract/tables.py, src/specforge_distill/models/candidates.py, src/specforge_distill/pipeline.py</files>
  <action>Implement table extraction using `pdfplumber` and emit requirement-like candidates from cells/rows with source metadata (`page`, `table_id`, `cell_ref`) and source type `table_cell`. Preserve broad capture behavior and keep uncertain candidates instead of dropping them.</action>
  <verify><automated>python -m pytest tests/phase1/test_table_and_caption_extraction.py::test_table_requirements_are_extracted_with_cell_origin -q</automated></verify>
  <done>Table-derived candidates are emitted with stable source typing and location metadata.</done>
</task>

<task type="auto">
  <name>Task 2: Implement caption-context candidate extraction</name>
  <files>src/specforge_distill/extract/captions.py, src/specforge_distill/pipeline.py</files>
  <action>Extract caption/figure-near text windows and emit candidates tagged `caption_context`. Include bounded nearby context windows and unknown-domain-verb flags when terms appear outside configured taxonomy.</action>
  <verify><automated>python -m pytest tests/phase1/test_table_and_caption_extraction.py::test_caption_context_candidates_include_flags -q</automated></verify>
  <done>Caption-context candidates exist with context windows and flagging behavior consistent with context decisions.</done>
</task>

<task type="auto">
  <name>Task 3: Add cross-source link merge and tests</name>
  <files>src/specforge_distill/extract/merge.py, tests/phase1/test_table_and_caption_extraction.py</files>
  <action>Implement merge process that preserves all candidates while creating semantic-link records for equivalent statements across source buckets. Do not dedupe away candidates in Phase 1; only attach cross-reference links for later phases.</action>
  <verify><automated>python -m pytest tests/phase1/test_table_and_caption_extraction.py -q</automated></verify>
  <done>Pipeline output includes linked duplicates across channels while retaining each candidate record.</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] `python -m pytest tests/phase1/test_table_and_caption_extraction.py -q`
- [ ] Candidate ledger contains source types `table_cell` and `caption_context`
- [ ] Duplicate-equivalent entries remain preserved with linkage metadata
</verification>

<success_criteria>

- Requirements in tables and caption contexts are captured
- Source-typed extraction channels remain independently diagnosable
- Merge strategy links equivalents without early dedupe loss
- Unknown obligation verbs are flagged, not discarded

</success_criteria>

<output>
After completion, create `.planning/phases/01-extraction-and-provenance-foundation/01-03-SUMMARY.md`
</output>
