---
phase: 05-trusted-distribution-and-install-verification
plan: 02
type: execute
wave: 2
depends_on: ["05-01"]
files_modified:
  - README.md
  - docs/BUILD.md
  - docs/TROUBLESHOOTING.md
  - src/specforge_distill/automation.py
  - src/specforge_distill/cli.py
  - tests/phase1/test_ingest_and_quality.py
  - tests/reliability/test_release_and_wrapper_contracts.py
autonomous: true
requirements: [REL-01, REL-02]
user_setup: []
must_haves:
  truths:
    - "Every documented binary install path requires checksum verification before first execution."
    - "Every documented binary install path requires `--version` and `--self-test` before processing a real PDF."
    - "Install-verification failures expose a stable failure class and a concise recovery hint that routes users to the right troubleshooting section."
  artifacts:
    - path: "README.md"
      provides: "Checksum-first and self-test-first install guidance"
    - path: "docs/TROUBLESHOOTING.md"
      provides: "Failure-class-first recovery guidance"
    - path: "src/specforge_distill/automation.py"
      provides: "Stable install-verification failure contract"
    - path: "src/specforge_distill/cli.py"
      provides: "Structured self-test failure output"
  key_links:
    - from: "README.md"
      to: "docs/TROUBLESHOOTING.md"
      via: "Install instructions route detailed recovery to troubleshooting by failure class"
      pattern: "Troubleshooting Guide"
    - from: "src/specforge_distill/cli.py"
      to: "src/specforge_distill/automation.py"
      via: "CLI returns structured install-verification failures from the automation contract"
      pattern: "failure_class"
    - from: "tests/phase1/test_ingest_and_quality.py"
      to: "src/specforge_distill/cli.py"
      via: "Tests verify structured self-test failure behavior"
      pattern: "self-test"
---

<objective>
Make the binary install path checksum-first, self-test-first, and explicit about failure recovery.

Purpose: Turn the current release binaries into a repeatable install flow users can trust in restricted and high-assurance environments.
Output: Updated docs and CLI failure payloads that guide users through download verification, first-run verification, and failure-class-based recovery.
</objective>

<execution_context>
@../../.codex/get-shit-done/workflows/execute-plan.md
@../../.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05-trusted-distribution-and-install-verification/05-CONTEXT.md
@.planning/phases/05-trusted-distribution-and-install-verification/05-RESEARCH.md
@README.md
@docs/BUILD.md
@docs/TROUBLESHOOTING.md
@src/specforge_distill/automation.py
@src/specforge_distill/cli.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: Rewrite install guides around the trust sequence</name>
  <files>README.md, docs/BUILD.md, docs/TROUBLESHOOTING.md</files>
  <action>
    - Update the main binary download flow for Ubuntu/WSL, macOS, and PowerShell 7 so every path uses the official GitHub Releases channel, verifies the checksum, runs `--version`, runs `--self-test`, and only then proceeds to a real PDF.
    - Use the versioned asset naming contract from 05-01 and keep platform-specific commands copy-pasteable.
    - Reorganize troubleshooting so the primary grouping is by failure class or symptom family, then give platform-specific recovery steps underneath those sections.
  </action>
  <verify>
    <automated>python3 verify_markdown.py README.md docs/TROUBLESHOOTING.md docs/BUILD.md</automated>
    <manual>Read the install sections in order and confirm users cannot reach a "run the binary on real input" step before checksum plus self-test.</manual>
    <sampling_rate>After implementation</sampling_rate>
  </verify>
  <done>All user-facing binary install docs follow the official-download -> checksum -> version -> self-test sequence.</done>
</task>

<task type="auto">
  <name>Task 2: Expose structured install-verification failures in the CLI</name>
  <files>src/specforge_distill/automation.py, src/specforge_distill/cli.py, tests/phase1/test_ingest_and_quality.py</files>
  <action>
    - Extend the self-test and related install-verification error payloads so they include a stable `failure_class`, a short `recovery_hint`, and a deterministic pointer to the troubleshooting guidance.
    - Keep the failure-class vocabulary aligned with `--describe-output json`; do not invent a separate install-only taxonomy.
    - Preserve existing success-path JSON formats unless the change is needed to make the recovery contract explicit and machine-readable.
  </action>
  <verify>
    <automated>PYTHONPATH=src .venv/bin/python3 -m pytest tests/phase1/test_ingest_and_quality.py -k "self_test or describe_output_json"</automated>
    <manual>Run a representative self-test failure locally by forcing an invalid output path or mocked validation failure and inspect the stderr JSON.</manual>
    <sampling_rate>After implementation</sampling_rate>
  </verify>
  <done>Install-verification failures are machine-readable and point users to a concrete recovery path.</done>
</task>

<task type="auto">
  <name>Task 3: Add contract tests for the secure install story</name>
  <files>tests/reliability/test_release_and_wrapper_contracts.py</files>
  <action>
    - Add lightweight reliability assertions that the README and troubleshooting docs still include checksum-first and self-test-first guidance, plus the official GitHub Releases channel restriction.
    - Keep these tests stable by checking for contract markers and command presence rather than fragile prose snippets.
    - Reuse existing release-contract test patterns instead of adding a separate test module unless the current file becomes too broad.
  </action>
  <verify>
    <automated>PYTHONPATH=src .venv/bin/python3 -m pytest tests/reliability/test_release_and_wrapper_contracts.py -k "install or release"</automated>
    <manual>None</manual>
    <sampling_rate>After implementation</sampling_rate>
  </verify>
  <done>The checksum-first install path and structured recovery guidance are guarded by tests as well as docs.</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] python3 verify_markdown.py README.md docs/TROUBLESHOOTING.md docs/BUILD.md
- [ ] PYTHONPATH=src .venv/bin/python3 -m pytest tests/phase1/test_ingest_and_quality.py -k "self_test or describe_output_json"
- [ ] PYTHONPATH=src .venv/bin/python3 -m pytest tests/reliability/test_release_and_wrapper_contracts.py -k "install or release"
</verification>

<success_criteria>
- The install path explicitly requires checksum verification and self-test.
- Failure handling is organized by failure class with platform-specific recovery beneath it.
- Self-test failure output includes a stable machine-readable recovery contract.
- Tests protect the secure install story from doc or CLI drift.
</success_criteria>

<output>
After completion, create .planning/phases/05-trusted-distribution-and-install-verification/05-02-SUMMARY.md
</output>
