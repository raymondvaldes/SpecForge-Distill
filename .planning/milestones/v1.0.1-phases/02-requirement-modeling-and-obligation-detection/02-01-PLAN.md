---
phase: 02-requirement-modeling-and-obligation-detection
plan: 01
type: execute
wave: 1
depends_on: []
files_modified: [pyproject.toml, src/specforge_distill/models/requirement.py]
autonomous: true
requirements: [ART-02]

must_haves:
  truths:
    - "A canonical Requirement model exists and validates its fields using Pydantic"
    - "Requirement instances can store VCRM attributes (method, rationale, allocation)"
    - "Candidates can be transformed into Requirement records without data loss"
  artifacts:
    - path: "src/specforge_distill/models/requirement.py"
      provides: "Pydantic-based Requirement and VCRMAttributes models"
  key_links:
    - from: "src/specforge_distill/models/requirement.py"
      to: "src/specforge_distill/models/candidates.py"
      via: "conversion method or factory"
      pattern: "from_candidate"
---

<objective>
Define the canonical requirement schema and VCRM-rebuild attributes using Pydantic. This moves the system from raw extraction "candidates" to formal, validated "Requirement" entities.

Purpose: Provide a type-safe, validated structure for requirements that supports identity tracking and verification metadata.
Output: Pydantic models for Requirement and VCRMAttributes.
</objective>

<execution_context>
@/Users/raymondvaldes/.gemini/get-shit-done/workflows/execute-plan.md
@/Users/raymondvaldes/.gemini/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/phases/02-requirement-modeling-and-obligation-detection/02-RESEARCH.md
@src/specforge_distill/models/candidates.py
@src/specforge_distill/provenance/models.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add Pydantic dependency</name>
  <files>pyproject.toml</files>
  <action>
    Add `pydantic >= 2.0.0` to the `dependencies` section in `pyproject.toml`.
    Run `pip install pydantic` or ensure the environment is updated.
  </action>
  <verify>
    <automated>python -c "import pydantic; print(pydantic.VERSION)"</automated>
    <manual>Check pyproject.toml contains pydantic</manual>
    <sampling_rate>once after commit</sampling_rate>
  </verify>
  <done>Pydantic is available in the environment and declared in pyproject.toml.</done>
</task>

<task type="auto">
  <name>Task 2: Define Requirement and VCRMAttributes Pydantic models</name>
  <files>src/specforge_distill/models/requirement.py</files>
  <action>
    Create `src/specforge_distill/models/requirement.py`.
    Implement `VCRMAttributes` class as a Pydantic `BaseModel` with fields: `method` (str, optional), `rationale` (str, optional), `allocation` (str, optional), `success_criteria` (str, optional).
    Implement `Requirement` class as a Pydantic `BaseModel` with fields:
    - `id`: str
    - `text`: str
    - `obligation`: str (shall, must, should, etc.)
    - `page`: int
    - `is_ambiguous`: bool = False
    - `ambiguity_reasons`: List[str] = []
    - `vcrm`: VCRMAttributes = Field(default_factory=VCRMAttributes)
    - `source_type`: str
    - `provenance`: Optional[Citation] (imported from specforge_distill.provenance.models)
  </action>
  <verify>
    <automated>python -c "from specforge_distill.models.requirement import Requirement; print(Requirement.model_json_schema())"</automated>
    <manual>Review Requirement model structure for coverage of VCRM and provenance</manual>
    <sampling_rate>after task</sampling_rate>
  </verify>
  <done>Requirement and VCRMAttributes models are defined with correct types and defaults.</done>
</task>

<task type="auto">
  <name>Task 3: Implement Candidate to Requirement conversion</name>
  <files>src/specforge_distill/models/requirement.py</files>
  <action>
    Add a static method `from_candidate(candidate: Candidate) -> Requirement` to the `Requirement` class.
    Ensure it maps `id`, `text`, `page`, `source_type`, and `provenance` correctly.
    Defaults `obligation` to "unknown" or "neutral" for now (to be filled by classifier later).
  </action>
  <verify>
    <automated>python -c "from specforge_distill.models.requirement import Requirement; from specforge_distill.models.candidates import Candidate; c = Candidate(id='1', text='X', source_type='narrative', page=1); r = Requirement.from_candidate(c); print(r.id == '1')"</automated>
    <manual>Verify provenance is preserved during conversion</manual>
    <sampling_rate>after task</sampling_rate>
  </verify>
  <done>Requirements can be instantiated from Candidate objects while preserving core data.</done>
</task>

</tasks>

<success_criteria>
1. `Requirement` and `VCRMAttributes` models are implemented and pass Pydantic validation.
2. Conversion from `Candidate` to `Requirement` is implemented and preserves provenance.
3. VCRM-rebuild attributes are included in the schema (ART-02).
</success_criteria>

<output>
After completion, create `.planning/phases/02-requirement-modeling-and-obligation-detection/02-01-SUMMARY.md`
</output>
