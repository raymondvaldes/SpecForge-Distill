---
phase: 03-output-packaging-and-interop-hooks
plan: 03-02
type: execute
wave: 1
depends_on: []
files_modified: [src/specforge_distill/render/manifest.py, tests/phase3/test_manifest_writer.py]
autonomous: true
requirements: [OUT-05]
must_haves:
  truths:
    - "Tool generates a JSON manifest indexing all output files and entities"
    - "Manifest includes links between requirements and their containing files/pages"
    - "Manifest follows a structured Pydantic-validated schema"
  artifacts:
    - path: "src/specforge_distill/render/manifest.py"
      provides: "Manifest generation and JSON serialization"
    - path: "tests/phase3/test_manifest_writer.py"
      provides: "Unit tests for manifest schema and content"
  key_links:
    - from: "src/specforge_distill/render/manifest.py"
      to: "src/specforge_distill/models/requirement.py"
      via: "indexing entities"
---

<objective>
Implement the manifest writer to provide a machine-readable index of the distillation results, facilitating downstream tool integration and RAG indexing.

Purpose: Enable automation and traceability by mapping internal entities to their external file locations.
Output: `ManifestWriter` class and Pydantic models for the manifest.
</objective>

<execution_context>
@/Users/raymondvaldes/.gemini/get-shit-done/workflows/execute-plan.md
@/Users/raymondvaldes/.gemini/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/phases/03-output-packaging-and-interop-hooks/03-RESEARCH.md
@src/specforge_distill/models/requirement.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: Define Manifest models and implement ManifestWriter</name>
  <files>src/specforge_distill/render/manifest.py</files>
  <action>
    - Define a `Manifest` Pydantic model (and sub-models like `ManifestEntity`, `ManifestFiles`).
    - Implement `ManifestWriter` that takes `PipelineResult` and a mapping of generated filenames.
    - Fields should include:
        - `manifest_version`
        - `source_pdf`
        - `generated_files` (dictionary of type to filename)
        - `entities`: List of requirement and artifact objects with their ID, type, page, and target file.
  </action>
  <verify>
    Instantiate the model with sample data and verify `model_dump_json()` produces the expected structure.
  </verify>
  <done>
    `ManifestWriter` produces valid JSON according to the research schema.
  </done>
</task>

<task type="auto">
  <name>Task 2: Implement manifest unit tests</name>
  <files>tests/phase3/test_manifest_writer.py</files>
  <action>
    Create unit tests to verify:
    - Schema validation (missing required fields should fail).
    - Correct indexing of entities from a mock result.
    - File links correctly reflect the provided file mapping.
  </action>
  <verify>
    pytest tests/phase3/test_manifest_writer.py
  </verify>
  <done>
    Manifest generation is verified and schema-compliant.
  </done>
</task>

</tasks>

<verification>
Verify that the `entities` list in the manifest includes both requirements and architecture artifact blocks.
</verification>

<success_criteria>
1. `ManifestWriter` produces a valid `manifest.json`.
2. All entities in the pipeline result are indexed in the manifest.
3. Manifest schema passes Pydantic validation.
</success_criteria>

<output>
After completion, create `.planning/phases/03-output-packaging-and-interop-hooks/03-02-SUMMARY.md`
</output>
