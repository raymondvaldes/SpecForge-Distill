---
phase: 02-requirement-modeling-and-obligation-detection
plan: 04
type: execute
wave: 3
depends_on: [02-02, 02-03]
files_modified: [src/specforge_distill/pipeline.py, tests/phase2/test_requirement_modeling.py, tests/phase2/fixtures/requirements.yaml]
autonomous: true
requirements: [REQ-03, REQ-04, REQ-05, REQ-06, ART-02]

must_haves:
  truths:
    - "Pipeline transforms raw candidates into Requirement records"
    - "Normalized requirements have stable IDs, obligations, and flags"
    - "Regression tests pass for all Phase 2 requirements"
  artifacts:
    - path: "tests/phase2/test_requirement_modeling.py"
      provides: "Requirement normalization acceptance tests"
  key_links:
    - from: "src/specforge_distill/pipeline.py"
      to: "src/specforge_distill/models/requirement.py"
      via: "normalization stage"
      pattern: "normalize_requirements"
---

<objective>
Build requirement normalization tests with edge-case fixtures and wire the normalization stage into the pipeline.

Purpose: Integrate ID resolution and classification (REQ-03, REQ-04, REQ-05, REQ-06) into the main pipeline and verify with regression tests.
Output: Requirement normalization tests and updated pipeline.
</objective>

<execution_context>
@/Users/raymondvaldes/.gemini/get-shit-done/workflows/execute-plan.md
@/Users/raymondvaldes/.gemini/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/phases/02-requirement-modeling-and-obligation-detection/02-RESEARCH.md
@src/specforge_distill/pipeline.py
@src/specforge_distill/extract/id_resolver.py
@src/specforge_distill/extract/classifier.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create edge-case requirement fixtures</name>
  <files>tests/phase2/fixtures/requirements.yaml</files>
  <action>
    Create `tests/phase2/fixtures/requirements.yaml`.
    Include cases:
    - Requirement with source ID (REQ-001)
    - Requirement without source ID (needs hashing)
    - Ambiguous requirement (passive voice, vague words)
    - Requirement with multiple obligation verbs
    - Neutral text that shouldn't be a requirement
  </action>
  <verify>
    <automated>ls tests/phase2/fixtures/requirements.yaml</automated>
    <manual>Review fixture coverage for ID, obligation, and ambiguity edge cases</manual>
    <sampling_rate>after task</sampling_rate>
  </verify>
  <done>Edge-case fixtures provide comprehensive coverage for normalization logic.</done>
</task>

<task type="auto">
  <name>Task 2: Wire Requirement Normalization into Pipeline</name>
  <files>src/specforge_distill/pipeline.py</files>
  <action>
    Implement `normalize_requirements(candidates: List[Candidate]) -> List[Requirement]` stage.
    Logic:
    - Convert `Candidate` to `Requirement`.
    - Resolve ID using `id_resolver`.
    - Enrich using `classifier`.
    - Update the pipeline to include this normalization pass after extraction and merge.
  </action>
  <verify>
    <automated>python -c "from specforge_distill.pipeline import normalize_requirements; from specforge_distill.models.candidates import Candidate; c = Candidate(id='tmp', text='Shall be fast.', source_type='narrative', page=1); r = normalize_requirements([c])[0]; print(r.obligation)"</automated>
    <manual>Review pipeline stage order</manual>
    <sampling_rate>after task</sampling_rate>
  </verify>
  <done>Pipeline now emits formal Requirement records instead of raw Candidates.</done>
</task>

<task type="auto">
  <name>Task 3: Add Requirement Modeling acceptance tests</name>
  <files>tests/phase2/test_requirement_modeling.py</files>
  <action>
    Create `tests/phase2/test_requirement_modeling.py`.
    Assert:
    - ID preservation for `REQ-001` format.
    - ID stability for anonymous text (same text/page = same ID).
    - Obligation classification accuracy.
    - Ambiguity flagging (is_ambiguous is True when it should be).
    - VCRM attribute field existence in Requirement.
  </action>
  <verify>
    <automated>pytest tests/phase2/test_requirement_modeling.py</automated>
    <manual>Verify all tests pass with edge-case fixtures</manual>
    <sampling_rate>after task</sampling_rate>
  </verify>
  <done>Phase 2 acceptance tests verify all requirement modeling requirements (REQ-03/04/05/06, ART-02).</done>
</task>

</tasks>

<success_criteria>
1. Pipeline transforms candidates into formal Requirement records (REQ-03/04/05/06).
2. All acceptance tests pass for requirement normalization and identity preservation.
3. Edge-case fixtures cover IDs, obligations, and ambiguity patterns.
</success_criteria>

<output>
After completion, create `.planning/phases/02-requirement-modeling-and-obligation-detection/02-04-SUMMARY.md`
</output>
